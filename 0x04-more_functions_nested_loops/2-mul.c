#include "holberton.h"
/**
 *mul - function that multiplies two integers and returns the result
 *@a: variable a to mul two integers
 *@b: variable n2 to mul two integers
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int r;

	r = a * b;
	return (r);
}
