#include "holberton.h"
/**
 * set_string - sets the value of a pointer to a char.
 * @s:the pointer that have the addres with the string
 * @to ponter where tha value where change
 *
 * Return: Always 0 (Success)
 */
void set_string(char **s, char *to)
{
	*s = to; 
}
